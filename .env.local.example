# Mux Signing Keys (Dashboard -> Settings -> Signing Keys)
# Required for signed video playback. If absent, falls back to unsigned (public).
MUX_SIGNING_KEY=your_signing_key_id
MUX_PRIVATE_KEY=your_base64_encoded_private_key
